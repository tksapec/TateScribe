<Window x:Class="TateScribe.App.ChatGptProofreadingPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ChatGPTへの指示"
        Width="760"
        Height="520"
        MinWidth="560"
        MinHeight="380"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
      <TextBlock VerticalAlignment="Center" Margin="0,0,8,0" Text="作業の種類:" />
      <ComboBox x:Name="TaskTypeSelector"
                Width="220"
                SelectionChanged="TaskTypeChanged">
        <ComboBoxItem Tag="TextProofreading" Content="本文校正" IsSelected="True" />
        <ComboBoxItem Tag="RubyAnnotation" Content="ルビ確認" />
      </ComboBox>
    </StackPanel>

    <TextBlock Grid.Row="1"
               x:Name="TaskDescription"
               Margin="0,0,0,10"
               TextWrapping="Wrap"
               Text="校正用パッケージと一緒にChatGPTへ渡します。返却された構造化テキストはTateScribeへ取り込みます。" />

    <TextBox x:Name="PromptEditor"
             Grid.Row="2"
             AcceptsReturn="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Disabled"
             FontSize="15"
             Padding="10" />

    <TextBlock x:Name="CopyStatus"
               Grid.Row="3"
               Margin="0,8,0,0"
               Foreground="DarkGreen"
               TextWrapping="Wrap" />

    <StackPanel Grid.Row="4"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,12,0,0">
      <Button Padding="14,7"
              Margin="0,0,8,0"
              Click="ResetPrompt">既定文に戻す</Button>
      <Button Padding="14,7"
              Margin="0,0,8,0"
              Click="CopyPrompt">クリップボードにコピー</Button>
      <Button Padding="14,7"
              Click="CloseWindow"
              IsCancel="True">閉じる</Button>
    </StackPanel>
  </Grid>
</Window>
